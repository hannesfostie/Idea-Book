<%= simple_form_for(@idea, :html => {:class => 'form-horizontal' }) do |f| %>
  <%= f.association :project %>
  <%= f.input :title %>
  <%= f.input :pitch, as: :text %>
  <%= f.input :added_info %>
  <%= f.button :submit %>
<% end %>



